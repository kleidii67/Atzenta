# Temporary Python files (no need to put them in Git)
__pycache__/
*.pyc

# SECRETS - must NEVER be uploaded to GitHub!
# (Comments MUST be on their own line - otherwise the rule doesn't work.)

# secret.key = the key that signs/encrypts the sessions
secret.key

# order.key = the key that encrypts the orders (Fernet)
order.key

# *.db = the databases (secret numbers + codes)
*.db

# Product images uploaded by managers (user content)
static/uploads/
